apiVersion: v1
kind: Service
metadata:
  name: hello-helm
  labels:
    app: hello-helm
spec:
  type: {{ .Values.service.type }}
  selector:
    app: hello-helm
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
