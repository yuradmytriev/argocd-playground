apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-waves-config
  annotations:
    # Wave 0 — created first
    argocd.argoproj.io/sync-wave: "0"
data:
  MESSAGE: "Created in wave 0, before the deployment"
