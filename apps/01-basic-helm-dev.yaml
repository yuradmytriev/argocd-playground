apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 01-basic-helm-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/yuradmytriev/argocd-playground.git
    targetRevision: HEAD
    path: 01-basic-helm
    helm:
      # values.yaml (base) + values-dev.yaml (dev overrides).
      # Later files win on key conflicts.
      valueFiles:
        - values.yaml
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: playground-helm-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
